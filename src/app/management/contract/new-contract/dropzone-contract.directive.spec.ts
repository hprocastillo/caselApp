import { DropzoneContractDirective } from './dropzone-contract.directive';

describe('DropzoneContractDirective', () => {
  it('should create an instance', () => {
    const directive = new DropzoneContractDirective();
    expect(directive).toBeTruthy();
  });
});
